import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "KAEL - Performance Marketing Agency",
  description: "We build performance marketing systems that turn ad spend into predictable revenue. Scale smarter. Grow profitably.",
  keywords: ["performance marketing", "growth agency", "meta ads", "google ads", "scaling"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
